function triangular( n ) {
  let sum = 0;
  for (let i = 1; i <= n; i++){
    sum = sum + i;
  }
  return sum;
}

function triangular( n ) {
   return (n > 0) ? ((n * n) + n) / 2 : 0;
}

function triangular( n ) {
   return (n > 0) ? n * (n + 1) / 2 : 0;
}
