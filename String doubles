function doubles(s){
let res = [];
  for (let el of s) {
    if (res[res.length-1] === el){
      res.pop();
    } else {
      res.push(el)
    }
  } return res.join('')
}

/*
function doubles(s){
    for (let i = 0;  i < s.length-1; i++) {
      if (s[i] === s[i + 1]) return doubles(s.slice(0,i) + s.slice(i+2));
    }
  return s;
}
*/
